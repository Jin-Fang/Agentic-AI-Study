{
  "name": "agentic-ai-study-books",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Build the four textbooks in this repo to self-contained HTML and PDF (mermaid diagrams rendered to SVG).",
  "scripts": {
    "build": "node build-books.mjs",
    "build:html": "node build-books.mjs --html-only",
    "build:pdf": "node build-books.mjs --pdf-only"
  },
  "dependencies": {
    "marked": "^12.0.0",
    "mermaid": "^11.0.0"
  }
}
